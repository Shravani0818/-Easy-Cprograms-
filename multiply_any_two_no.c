#include<stdio.h>
void main()
{
    int a,b,multiply;
    printf("enter first number:");
    scanf(" %d",&a);
     printf("enter second number:");
    scanf (" %d",&b);
    multiply=a*b;
    printf(" sum=%d",multiply);
}